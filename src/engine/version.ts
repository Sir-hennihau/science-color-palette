/** Package-style version of the engine implementation. */
export const ENGINE_VERSION = '0.1.0'

/**
 * Bumped whenever a change alters generated output. Golden snapshots are keyed
 * on this, so a bump is the signal to review and re-record them.
 */
export const ALGORITHM_VERSION = 2
